// import functions and grab DOM elements
const input = document.getElementById('rock');
const input = document.getElementById('paper');
const input = document.getElementById('scissors');
const button = document.getElementById('throw');
// initialize state

// set event listeners to update state and DOM
